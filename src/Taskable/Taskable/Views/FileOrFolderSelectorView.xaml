<Window x:Class="TaskableApp.Views.FileOrFolderSelectorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskableApp.Views"
        mc:Ignorable="d" Closed="Window_Closed" ContentRendered="Window_ContentRendered"
        Title="FileOrFolderSelectorView" Height="150" Width="450">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Source="/Images/file.png" Width="32" Height="32" Grid.RowSpan="2" Margin="20,0" />

        <Label Grid.Column="1">Assembly:</Label>
        <TextBox Name="TxtFileOrFolder" Grid.Column="1" Grid.Row="1" MinWidth="250" Text="{Binding FileOrFolder}"></TextBox>

        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button MinWidth="60" Margin="0,0,10,0" Command="{Binding SaveCommand}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Images/icon-save.gif" Height="16" Width="16" />
                    <TextBlock Margin="5,0,0,0" Text="Add Parameter" />
                </StackPanel>
            </Button>
            <Button IsCancel="True" MinWidth="60">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Images/errors.png" Height="16" Width="16" />
                    <TextBlock Margin="5,0,0,0" Text="Cancel" />
                </StackPanel>
            </Button>
        </WrapPanel>
    </Grid>
</Window>
