<UserControl x:Class="TaskableApp.Views.CodeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskableApp.Views"
             xmlns:editor="clr-namespace:TaskableApp;assembly="
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <WrapPanel Orientation="Horizontal" Grid.Row="0">
            <Button Height="20" Width="20" Background="Transparent" BorderThickness="0" Command="{Binding SaveDocumentCommand}">
                <Image Source="/Images/icon-Save.gif" />
            </Button>
        </WrapPanel>
        <editor:CustomTextEditor x:Name="textEditor"
                                 SyntaxHighlighting="C#"
                                 FontFamily="Consolas"
                                 FontSize="10pt" 
                                 Document="{Binding Document}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 HorizontalScrollBarVisibility="Hidden"
                                 Grid.Row="1"
                                 Loaded="textEditor_Loaded"/>
    </Grid>
</UserControl>
